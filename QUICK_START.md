# 🚀 دليل البدء السريع - What You Missed

## التثبيت السريع

### 1. تشغيل الإعداد التلقائي
```bash
python run_tests.py
```

### 2. أو الإعداد اليدوي
```bash
# إعداد قاعدة البيانات
python setup_activity_tracker.py

# اختبار النظام (اختياري)
python test_activity_tracker.py

# تشغيل البوت
python bot.py
```

## الأوامر الأساسية

### للمستخدمين
```
!missed          # عرض الأنشطة المفقودة
!missed 48       # آخر 48 ساعة
!فاتني           # الأمر بالعربية
!recent          # آخر الأنشطة السريعة
!recent 15       # آخر 15 نشاط
```

### للإداريين
```
!activity status    # حالة النظام
!activity cleanup   # تنظيف البيانات القديمة
!activity stats     # إحصائيات النظام
```

## الواجهة التفاعلية

عند استخدام `!missed` ستحصل على واجهة تفاعلية مع:

- **📋 ملخص**: عرض مجمع للأنشطة
- **📝 تفاصيل**: عرض تفصيلي مع التنقل
- **◀️ ▶️**: التنقل بين الصفحات
- **🔄 تحديث**: تحديث البيانات
- **❌ إغلاق**: إغلاق الواجهة

## الأنشطة المتتبعة

- 👋 **الانضمام والمغادرة**: أعضاء جدد ومن غادروا
- 💬 **الرسائل المهمة**: الرسائل الطويلة والمرفقات
- 🔊 **الأنشطة الصوتية**: دخول وخروج من Voice Channels
- 🎮 **الألعاب**: بداية لعب ألعاب جديدة

## التخصيص

يمكنك تعديل الإعدادات في `activity_config.py`:

```python
# تغيير الحد الأقصى للساعات
MAX_HOURS = 168  # أسبوع

# تعطيل تتبع معين
TRACK_GAMES = False

# تغيير عدد العناصر في الصفحة
ITEMS_PER_PAGE = 15
```

## استكشاف الأخطاء

### المشكلة: البوت لا يتتبع الأنشطة
**الحل:**
1. تأكد من تحميل الـ cog: `cogs.activity_tracker`
2. تحقق من إعدادات السيرفر: `!activity status`

### المشكلة: لا توجد أنشطة مفقودة
**الحل:**
1. تأكد من وجود أنشطة في السيرفر
2. جرب فترة زمنية أطول: `!missed 48`

### المشكلة: خطأ في قاعدة البيانات
**الحل:**
1. شغل الإعداد مرة أخرى: `python setup_activity_tracker.py`
2. تحقق من صلاحيات الكتابة في المجلد

## الدعم

إذا واجهت أي مشاكل:
1. تحقق من ملف `ACTIVITY_TRACKER_README.md` للتفاصيل الكاملة
2. شغل الاختبارات: `python test_activity_tracker.py`
3. تواصل مع المطور: werjo

---

**نصائح سريعة:**
- استخدم `!recent` للأنشطة السريعة
- استخدم `!missed` للعرض التفاعلي الكامل
- الإداريون يمكنهم استخدام `!activity cleanup` لتنظيف البيانات القديمة