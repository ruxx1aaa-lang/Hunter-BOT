[phases.setup]
nixPkgs = ["ffmpeg", "python312", "libsodium", "libopus", "nodejs_20"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python bot.py"
